# API runtime
fastapi==0.139.0
uvicorn

# Model runtime — scikit-learn pinned to the version that trained the
# committed .pkl so the pipeline loads identically everywhere.
scikit-learn==1.9.0
pandas==3.0.3
joblib==1.5.3

# Shared domain package from this monorepo:
#   pip install -e ../../packages/core_py

# Tests only
httpx
