# Serverless API (Vercel Python runtime). Only fastapi: the function is
# self-contained (no scikit-learn/pandas and no tasador-core install),
# because Vercel's uv builder cannot install the monorepo package. The
# domain logic is inlined in api/index.py and kept honest by
# tests/test_serverless_parity.py (compares it against tasador-core in CI).
fastapi
